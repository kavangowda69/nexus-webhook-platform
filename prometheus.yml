global:
  scrape_interval: 5s

scrape_configs:
  - job_name: webhook_api
    static_configs:
      - targets: ["webhook_api:8000"]

  - job_name: webhook_worker
    static_configs:
      - targets: ["webhook_worker:9090"]